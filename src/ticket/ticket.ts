import {Module}         from './../module';
import {Bot}            from './../bot';

export class Tickets implements Module {
    commands = [
    ];
    register(bot: Bot) 
    {
    }
    name = "Tickets";

    public config = {};
};