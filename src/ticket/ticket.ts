import {Module}         from './../module';
import {Bot}            from './../bot';

export class Utility implements Module {
    commands = [
    ];
    register(bot: Bot) 
    {
    }
    name = "Utility";

    public config = {};
};